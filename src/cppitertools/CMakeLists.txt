
set( TARGET_NAME cppitertools )
set( TARGET_VERSION ${${CMAKE_PROJECT_NAME}_VERSION} )
set( TARGET_EXPORT ${CMAKE_PROJECT_NAME}Targets )

install(
    DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
    DESTINATION ${INSTALL_INCDIR}
    FILES_MATCHING
        PATTERN "*.h*"
        PATTERN "LICENSE.md" )
